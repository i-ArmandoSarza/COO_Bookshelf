<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.COO_Bookshelf">
    <activity
      android:exported="false"
      android:name=".booksearch.MyBookSearchActivity" />
    <activity
      android:exported="false"
      android:name=".AboutActivity" />
    <activity
      android:exported="false"
      android:name=".admin.AdminSettings" />
    <activity
      android:exported="false"
      android:name=".admin.AdminRemoveUser" />
    <activity
      android:exported="false"
      android:name=".AdditionalUserInfoActivity" />
    <activity
      android:exported="false"
      android:name=".SignUpActivity" />
    <activity
      android:exported="false"
      android:name=".MainActivity" />
    <activity
      android:exported="true"
      android:name=".LoginPageActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".mybooks.MyBookActivity" />
  </application>
  <uses-permission android:name="android.permission.INTERNET" />

</manifest>